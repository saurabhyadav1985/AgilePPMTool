version: 2.1

orbs:
  maven: circleci/maven@3.6.3

parameters:
  app_src_directory: 
    default : '/ppmtool'
    type: 'string'
    
workflows:
  maven_test:
    jobs:
      - maven/test:
          test_results_path: /test_results
